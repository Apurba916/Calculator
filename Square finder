#include <stdio.h>
#include <math.h>
int main(){
    double n;
    printf("Enter a number:\n");
    scanf("%lf", &n);
    double square = pow(n, 2);
    printf("The square of %lf is %lf.", n, square);
    return 0;
}
